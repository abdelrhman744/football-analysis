# ── Core FastAPI stack ────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18
pydantic==2.10.3
httpx==0.28.1
python-dotenv==1.0.1

# ── CV / AI packages (from the Kaggle notebook) ───────────────────────────────
# Install these when you are ready to use the real model.
# They are commented out so the backend still starts without a GPU or heavy deps.
#
# opencv-python==4.10.0.84        # video reading, frame processing, heatmap colormap
# ultralytics==8.3.53             # YOLOv8 model loading + predict()
# supervision==0.24.0             # ByteTrack, annotators (BoundingBox, Label, Trace, Triangle)
# numpy>=1.24.0                   # array ops throughout cv/ package
# scipy>=1.11.0                   # gaussian heatmap (heatmap.py _to_normalised_matrix)
# torch>=2.0.0                    # PyTorch backend for ultralytics
# torchvision>=0.15.0
#
# To install the full CV stack:
#   pip install opencv-python ultralytics supervision numpy scipy torch torchvision
#
# For GPU (CUDA 12.x):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── Optional: team classification & chatbot ───────────────────────────────────
# scikit-learn>=1.3.0             # KMeans for team colour clustering
# Pillow>=10.0.0
# matplotlib>=3.8.0               # alternative heatmap rendering
# anthropic>=0.40.0               # Claude chatbot integration
# openai>=1.50.0                  # GPT-4 alternative
